<template name="membermgt">
{{#if showprofform}}
		{{>profileForm}}
{{/if}}
	
		<div class="page-header span9">
				<h1>Member Profile <small>Management</small></h1>
						
		</div>	
		
		<ul class="unstyled" id="postlist">
			{{#each Members}}
				{{>memberProfile}}
			 {{/each}}
		</ul>
	
</template>

<template name="membermgtProfile">
		<div class="membermgtProfile" id="membermgtProfile">
		<ul class="unstyled well">
			<li>
			
					<div class="media-body span3">
						<h4 class="media-heading">{{_id}}</h4>
					  	<p>
						
						{{#each emails}}
							{{address}}
						{{/each}}
						</p>
					</div>
			
	   		
			</li>
		</ul>		
		</div>

</template>

<template name="profileForm">
	<div id="addevent" name="addevent" class="modal-backdrop addevent">
			<div class="modal-header">
				<a class="close" data-dismiss="modal">x</a>
				<h4>Add Event</h4>				
			</div>
			<div class="modal-body">
	<ul class="unstyled">
				<label for="name">Event Name </label>
				<input class="input input-xlarge eventname" type="text" name="eventname" value="" id="eventname">

				<label for="name">Date & Time </label>
				<input class="input input-xlarge datentime" type="text" name="datentime" value="{{events.eventdatetime}}" id="datentime">

				
			<label for="name">Event Status </label>
			<select name="status" id="status" class="status">
				<option value="Future">Future</option>
				<option value="Coverd">Coverd</option>
			</select>
	
	</ul>
				
			</div>

			<div class="modal-footer">
				{{#if editing_event_data}}
					<a herf="" name="delete" id="delete" class="btn btn-danger delete">Delete</a>
					<a herf="" name="save" id="save" class="btn btn-warning save">Update</a>
				{{else}}
					<a herf="" name="save" id="save" class="btn btn-success save">Save</a>
				{{/if}}
					<a herf="" name="cancel" id="cancel" class="btn cancel" data-dismiss="modal">Cancel</a>			
			</div>
	</div>
</template>
